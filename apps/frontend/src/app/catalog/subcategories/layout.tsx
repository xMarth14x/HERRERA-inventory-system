import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Subcategories - BigStop Inventory",
};

export default function SubcategoriesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
