import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unit Conversions - BigStop Inventory",
};

export default function UnitConversionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
