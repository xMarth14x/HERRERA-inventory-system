import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Brands - BigStop Inventory",
};

export default function BrandsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
