import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Units of Measurement - BigStop Inventory",
};

export default function UnitsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
